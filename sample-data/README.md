# Sample Data

This folder is reserved for optional workbook samples used with:

```powershell
python scripts\import_workbook.py <path-to-xlsx>
```

No production workbook data is bundled in this standalone app. The seed loader generates realistic operational demo data directly into PostgreSQL.
